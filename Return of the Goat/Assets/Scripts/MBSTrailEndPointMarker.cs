using UnityEngine;

public class MBSTrailEndPointMarker : MonoBehaviour
{

    public Transform trnTrailStart;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
  
}
